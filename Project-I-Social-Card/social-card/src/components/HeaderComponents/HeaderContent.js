import React from 'react';
import './Header.css';


const HeaderContent = props => {
  return (
    <div>
      <p>Let's learn React by buiding simple interfaces with components.  Don't ry to overthink it, just keep it simple and have fun.  Once you feel comfortable using components you are well on you way to mastering React!</p>
    </div>
  );
};

// const MyButton = props => {
//   return <button className="my-fancy-button">Click this Button</button>;
// };

export default HeaderContent;